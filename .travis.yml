language: cpp

addons:
  apt:
    packages:
    - libblas-dev
    - liblapack-dev

compiler:
  - gcc

script:
  - make -e realclean &&
    make -e -j

  - cd samples/copy &&
    make -e realclean &&
    make -e &&
    cd ../..

  - cd samples/entropy &&
    make -e realclean &&
    make -e &&
    cd ../..

  - cd samples/multi-dgemm &&
    make -e realclean &&
    make -e &&
    cd ../..

  - cd samples/test &&
    make -e realclean &&
    make -e &&
    cd ../..
